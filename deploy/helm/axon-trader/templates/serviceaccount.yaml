{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "axon-trader.serviceAccountName" . }}
  labels:
    {{- include "axon-trader.labels" . | nindent 4 }}
{{- end }}
